function addEvent() {
  const day = prompt("¿Para qué día?");
  const task = prompt("¿Qué tarea?");
  alert(`Evento agregado: ${task} el día ${day}`);
  // Lógica real pendiente: guardar en JSON o localStorage
}
